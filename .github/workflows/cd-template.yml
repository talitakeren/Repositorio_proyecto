# Plantilla de despliegue continuo — SGOHA
#
# Este workflow NO se ejecuta automáticamente (sin secretos de despliegue).
# Activar cuando se configure Vercel (frontend) y/o Render (backend).
#
# Secretos requeridos (GitHub → Settings → Secrets):
#   VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID  — frontend
#   RENDER_DEPLOY_HOOK_URL                          — backend
#
# Copiar a cd.yml y descomentar jobs cuando los secretos estén listos.

name: CD Template (inactivo)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Escriba DEPLOY para confirmar"
        required: true

# Descomentar cuando CI y secretos estén configurados:
#
# on:
#   workflow_run:
#     workflows: ["CI SGOHA"]
#     types: [completed]
#     branches: [main]
#   workflow_dispatch:
#
# concurrency:
#   group: cd-production
#   cancel-in-progress: false
#
# jobs:
#   deploy-frontend:
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#     runs-on: ubuntu-latest
#     environment: production
#     steps:
#       - uses: actions/checkout@v4
#       - uses: amondnet/vercel-action@v25
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#           vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#           working-directory: frontend
#           vercel-args: "--prod"
#
#   deploy-backend:
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#     runs-on: ubuntu-latest
#     environment: production
#     steps:
#       - name: Trigger Render deploy hook
#         run: curl -fsS -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "CD no configurado. Ver docs/CI_CD_GITHUB_ACTIONS.md"
          echo "No existe vercel.json ni render.yaml en el repositorio."
          exit 0
